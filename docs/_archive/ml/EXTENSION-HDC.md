# ML Extension — HDC 4096D + Drift + Sophisticate

**HDC:** `quantum_utility_platform/` + `src/lib/native-ml/governed-ml.ts` `detectDrift(0.15)` + `auditFairness` + `measureVelocity` + `sophisticateReply` (lexicon + closings) integrado en `local-responder.ts` via `language-core`.

**Manejo:** `4096D` hypervectors, `binding`/`bundling`, decaimiento `0.995`, `AES-256-GCM` + `HMAC-SHA3-512`, `AEGIS Gate` `cosine>0.65`.

**Verificación:** `pnpm test` `545 passed` + `governed-ml.ts` `detectDrift`/`auditFairness`.
