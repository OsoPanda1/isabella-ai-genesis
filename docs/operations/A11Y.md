# A11Y — Accesibilidad WCAG 2.2 AA (P2)

**Objetivo:** `WCAG 2.2 AA` verificable en `pnpm test` + `axe-core`.

**Cobertura:**
- `Keyboard-only` — `Tab` navega `IsabellaClientApp`, `SovereignSkillsPanel`, `CommandLine`
- `Screen reader` — `aria-label` en `Starfield`, `CrystalNavigation`, `TerminalView`
- `Reduced motion` — `prefers-reduced-motion` en `CinematicIntro`, `Lenis`
- `Contrast` — `palette` `CKM Brand` `11.2:1` `WCAG AAA`
- `Focus` — `focus-visible` en `Radix UI` `Dialog`, `Dropdown`, `Tabs`
- `Form validation` — `zod` en `isabella-chat-gateway`, `billing`, `isabella-voice`
- `Error recovery` — `EmergencyModeView` + `CROWN Recovery` con `Health/Readiness`

**Tests:** `pnpm test --project unit` con `@testing-library/react` + `axe-core` (pendiente `CI`).

**Estado:** `implemented` — `verified` requiere `axe` en `CI` + `manual` con lector.
