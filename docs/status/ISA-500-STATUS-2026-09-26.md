# Estado de la auditoría ISA-500 — 2026-09-26

Fuente: `ISABELLA-GENESIS-500-CHECKLIST.{md,json,500}` (500 ítems, 134 P0), re-verificados contra el árbol de trabajo de `main` (`3502967` + correcciones posteriores).

Reglas de clasificación aplicadas (conservadoras):

- `FIXED`: el cambio descrito está implementado **y** existe prueba automatizada asociada.
- `PARTIAL`: existe código relacionado, pero el criterio de aceptación sólo se cumple parcialmente (p. ej. sin prueba, o sólo en algunos caminos).
- `STILL_BROKEN`: no hay implementación verificable.
- `BLOCKED_ENVIRONMENT`: no puede verificarse sin infraestructura viva (DB, GHCR, CI).

## Conteos

| Ámbito | FIXED | PARTIAL | STILL_BROKEN | BLOCKED | Total |
|---|---:|---:|---:|---:|---:|
| P0 | 35 (26.1%) | 56 (41.8%) | 39 (29.1%) | 4 (3.0%) | 134 |
| Total | 44 (8.8%) | 374 (74.8%) | 72 (14.4%) | 8 (1.6%) | 500 |

- **Implementación ponderada (cota inferior):** `(44×1 + 374×0.5) / 500 = 46.2%`.
- **Implementación estricta (sólo `FIXED`):** `44 / 500 = 8.8%` (P0: `35 / 134 = 26.1%`).
- **P0 ponderado:** `(35 + 56×0.5) / 134 = 47.0%`.
- `PARTIAL` es además el valor por defecto para los ítems P1/P2 no verificados en profundidad; por eso el 46% se declara **cota inferior**, no una medida fina.

`BLOCKED_ENVIRONMENT`: `ISA-234`, `ISA-372`–`ISA-378` (DB viva, clean-room, CI, GHCR).

## Ítems `FIXED` (44)

`ISA-026, 034, 041, 042, 043, 044, 045, 083, 084, 089, 090, 091, 092, 093, 094, 151, 152, 159, 164, 184, 185, 187, 199, 200, 201, 205, 206, 207, 210, 211, 220, 248, 275, 277, 280, 290, 291, 315, 336, 355, 385, 392, 394, 418`

`ISA-083`: sanitización de corpus al ingerir en `src/lib/ncua/lsh.ts` + guardia `isTrustedMemoryResponse()` en `src/lib/ncua/pipeline.ts`; `ISA-392`: `test/security/rag-poisoning.test.ts`.

Correcciones adicionales del mismo ciclo (verificadas con suite local verde, aunque el ítem asociado no figure como `FIXED` completo): bloqueo fail-closed de acciones dev en producción (`dev-auth-guard`), permisos `system:state`, aislamiento de tenant en el adaptador JSON, guard de signup, redacción de logs, salida de errores sin internos en 3 rutas, honestidad de datos en `territorial-twin`/`SovereignCompliancePanel`/`ApiCatalogExplorer`, y `process.env` centralizado en `src/lib/config.ts`.

## Principales ítems aún abiertos (P0)

1. `ISA-003/004/005/011/013/014/016/017` — no existe un MoE real (`src/lib/moe/*`); el router actual es scoring por palabras.
2. `ISA-140/175` — sin gate de salida (no existe `interceptResponse`/`scanOutput`); el SSE se emite sin escanear.
3. `ISA-125` — `governIntelligence()` es código muerto: el gateway no recorre el router de inteligencia.
4. `ISA-427/428` — sin borrado verificable ni cascada (memoria/auditoría/economía).
5. `ISA-325` — el gate de secretos/SAST sí existe y bloquea `deploy` vía `fgais-gate.yml` (`security:scan`), pero no es verificable desde aquí si la protección de rama lo exige como check obligatorio.
6. `ISA-379–384, 388, 391, 393` — suites de seguridad/excepción aún no escritas (runtime TINA, aislamiento de economía, inyección de salida, egress de PII). `ISA-392` (RAG poisoning) ya existe.
7. `ISA-449/448/461` — panel de cumplimiento: los porcentajes fabricados fueron retirados, pero sigue sin leer verificación del ledger.
8. `ISA-482` — `scripts/genesis-route-audit.mjs` no audita auth/rate-limit/validación por ruta.
9. `ISA-489` — sin modelo de amenazas en `docs/security/`.

## Verificación de esta corrida (local, 2026-09-26)

| Gate | Estado |
|---|---|
| `typecheck` | PASS (0 errores) |
| `lint` | PASS (0 errores, 47 warnings) |
| `test` | PASS — 676 pruebas, 13 omitidas, 0 fallos (115 archivos, 117 en total) |
| `build` | PASS (Vite + Nitro) |
| `security:scan` | PASS |
| `verify:lock` | PASS |
| `capabilities` | PASS |
| `audit:repository` / `audit:routes` | PASS |
| `db:verify` | PASS (sólo estático) |
| `production:integrity` / `production:preflight` | PASS |
| `format:check` | PASS |
| `k8s:image:verify` | BLOCKED_ENVIRONMENT (GHCR anónimo: `DENIED`) |
| `db:migrate` / RLS live | No ejecutado (sin `DATABASE_URL`) |
| `production:evidence` | EVIDENCE_GATED salvo con árbol limpio y commit same-commit |

`EVIDENCE_GATED` **no** cuenta como `PASS` (AGENTS §19).
