name: CI

on:
  workflow_call:

permissions:
  contents: read

permissions:
  contents: read

jobs:
<<<<<<< Updated upstream
  production-gate:
    name: Canonical FGAIS gate
    uses: ./.github/workflows/fgais-gate.yml
    permissions:
      contents: read
=======
  build_and_test:
    name: Continuous Integration Quality Gate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      attestations: write
      id-token: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Enable pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Linter (ESLint)
        run: pnpm run lint

      - name: Code Formatting Check
        run: pnpm run format:check

      - name: TypeScript Type Checking
        run: pnpm run typecheck

      - name: Run Unit Tests
        run: pnpm run test:unit

      - name: Run Security Tests
        run: pnpm run test:security

      - name: Dependency Vulnerability Audit (blocking)
        run: pnpm audit --audit-level=high

      - name: Secret Scan & Security Analysis
        run: pnpm run security:scan

      - name: Build Application
        run: pnpm run build

      - name: Generate CycloneDX SBOM
        run: pnpm dlx @cyclonedx/cyclonedx-npm --output-file sbom.cyclonedx.json

      - name: Attest SBOM (SLSA provenance)
        if: github.event_name == 'push'
        uses: actions/attest-sbom@v1
        with:
          subject-path: dist
          sbom-path: sbom.cyclonedx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx
          path: sbom.cyclonedx.json
          if-no-files-found: error
>>>>>>> Stashed changes
