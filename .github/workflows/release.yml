name: Production Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: "24.x"
  PNPM_VERSION: "10.15.0"
  RELEASE_TAG: ${{ github.ref_name }}

permissions:
  contents: write

jobs:
<<<<<<< Updated upstream
  env-check:
    name: Validate production contract
    runs-on: ubuntu-22.04
    steps:
      - name: Validate required production variables
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PUBLIC_URL: ${{ secrets.PUBLIC_URL }}
          ISABELLA_STORAGE_PROVIDER: ${{ secrets.ISABELLA_STORAGE_PROVIDER }}
          AUTH_JWT_SECRET: ${{ secrets.AUTH_JWT_SECRET }}
          ENCRYPTION_MASTER_KEY: ${{ secrets.ENCRYPTION_MASTER_KEY }}
          CROWN_POLICY_SIGNING_KEY: ${{ secrets.CROWN_POLICY_SIGNING_KEY }}
          AEGIS_AUDIT_SECRET: ${{ secrets.AEGIS_AUDIT_SECRET }}
          BOOKPI_SIGNING_KEY: ${{ secrets.BOOKPI_SIGNING_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ISABELLA_RUNTIME_MODE: ${{ secrets.ISABELLA_RUNTIME_MODE }}
          PROVISION_OWNER_TOKEN: ${{ secrets.PROVISION_OWNER_TOKEN }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
        run: |
          set -euo pipefail
          REQUIRED_VARS=(
            DATABASE_URL
            PUBLIC_URL
            ISABELLA_RUNTIME_MODE
            ISABELLA_STORAGE_PROVIDER
            AUTH_JWT_SECRET
            ENCRYPTION_MASTER_KEY
            CROWN_POLICY_SIGNING_KEY
            AEGIS_AUDIT_SECRET
            BOOKPI_SIGNING_KEY
            GEMINI_API_KEY
            PROVISION_OWNER_TOKEN
            STRIPE_SECRET_KEY
            STRIPE_WEBHOOK_SECRET
          )
          missing=0
          for name in "${REQUIRED_VARS[@]}"; do
            if [ -z "${!name:-}" ]; then
              echo "::error::Missing production secret/variable: $name"
              missing=1
            fi
          done
          if [ "$missing" -ne 0 ]; then exit 1; fi
          echo "Production environment contract satisfied: ${#REQUIRED_VARS[@]} variables present."

  verify:
    name: Re-run canonical production gate
    needs: env-check
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read

  release:
    name: Build and publish release
    needs: [env-check, verify]
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Production build
        env:
          NODE_ENV: production
        run: pnpm build

      - name: Validate Nitro output
        run: |
          set -euo pipefail
          test -d .output
          test -f .output/server/index.mjs
          find .output -type f -print0 | sort -z | xargs -0 sha256sum > release-sha256.txt

      - name: Package immutable release artifact
        run: |
          set -euo pipefail
          mkdir -p artifacts
          tar -czf "artifacts/isabella-gateway-${RELEASE_TAG}.tar.gz" .output package.json pnpm-lock.yaml
          sha256sum "artifacts/isabella-gateway-${RELEASE_TAG}.tar.gz" > "artifacts/isabella-gateway-${RELEASE_TAG}.tar.gz.sha256"
          cp release-sha256.txt "artifacts/release-output-sha256.txt"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          generate_release_notes: true
          files: artifacts/*
          prerelease: ${{ contains(env.RELEASE_TAG, '-rc') }}
          make_latest: ${{ !contains(env.RELEASE_TAG, '-rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
=======
  release_build:
    name: Build, Sign & Package Production Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Enable pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Compile and Bundle Production Build
        run: pnpm run build:production

      - name: Generate CycloneDX SBOM
        run: pnpm dlx @cyclonedx/cyclonedx-npm --output-file sbom.cyclonedx.json

      - name: Create Production Release Bundle
        run: |
          if [ -d ".output" ]; then
            tar -czf isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz .output/ dist/ package.json sbom.cyclonedx.json
          else
            tar -czf isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz dist/ package.json sbom.cyclonedx.json
          fi

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign artifact (cosign) and generate verification bundle
        if: secrets.COSIGN_PRIVATE_KEY != '' && secrets.COSIGN_PASSWORD != ''
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
          COSIGN_PUBLIC_KEY: ${{ secrets.COSIGN_PUBLIC_KEY }}
        run: |
          cosign sign-blob --yes --key env://COSIGN_PRIVATE_KEY isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz > isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz.sig

      - name: Verify signature (cosign)
        if: secrets.COSIGN_PRIVATE_KEY != '' && secrets.COSIGN_PASSWORD != '' && secrets.COSIGN_PUBLIC_KEY != ''
        env:
          COSIGN_PUBLIC_KEY: ${{ secrets.COSIGN_PUBLIC_KEY }}
        run: |
          cosign verify-blob \
            --key <(echo "$COSIGN_PUBLIC_KEY") \
            --signature isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz.sig \
            isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz

      - name: Verify SLSA provenance (attest-build-provenance)
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz

      - name: Publish GitHub Release Artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: |
            isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz
            isabella-cognitive-gateway-${{ github.ref_name }}.tar.gz.sig
            sbom.cyclonedx.json
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
>>>>>>> Stashed changes
